@{
	string[] names = { "Pavel", "Jana", "Veronika" };
}

<h1>Hello world!</h1>

<p>
	@DateTime.Now.ToString("dd. MM. yyyy")
</p>

@foreach(var name in names)
{
	var fullname = name + " XX";
	<p>@fullname</p>
}